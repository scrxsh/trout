import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-floating-cards',
  imports: [RouterLink],
  templateUrl: './floating-cards.html',
  styleUrl: './floating-cards.css',
})
export class FloatingCards {
  
}
